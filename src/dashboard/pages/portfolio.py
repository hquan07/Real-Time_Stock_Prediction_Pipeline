from dash import html, dcc

from dash import dash_table

from dashboard.layouts import COLORS, GLASS_CARD_STYLE, HEADER_STYLE

def create_portfolio_tab():
    """Create Portfolio & Signals tab content."""
    
    return html.Div(
        [
            # Top row - Portfolio Summary
            html.Div(
                [
                    html.H3(["💼 ", "Portfolio Overview"], style=HEADER_STYLE),
                    html.Div(
                        id="portfolio-summary",
                        children=[], # Will be populated by callback
                        style={
                            "display": "grid",
                            "gridTemplateColumns": "repeat(4, 1fr)",
                            "gap": "16px",
                            "marginBottom": "16px",
                        }
                    )
                ],
                style=GLASS_CARD_STYLE
            ),
            
            # Middle row - Asset Allocation & Performance
            html.Div(
                [
                    html.Div(
                        [
                            html.H3(["🥧 ", "Asset Allocation"], style=HEADER_STYLE),
                            dcc.Graph(id="asset-allocation-chart", config={"displayModeBar": False}, style={"height": "300px"}),
                        ],
                        style={**GLASS_CARD_STYLE, "flex": "1", "marginRight": "16px"}
                    ),
                    html.Div(
                        [
                            html.H3(["📈 ", "Portfolio Holdings"], style=HEADER_STYLE),
                            html.Div(id="portfolio-holdings-table") # Will be populated by callback
                        ],
                        style={**GLASS_CARD_STYLE, "flex": "2"}
                    ),
                ],
                style={"display": "flex", "marginBottom": "16px"}
            ),
            
            # Bottom row - Latest Signals
            html.Div(
                [
                    html.H3(["🚨 ", "Latest Trading Signals"], style=HEADER_STYLE),
                    html.P("Automated signals generated by ML & Technical Indicators.", style={"color": COLORS["text_muted"], "fontSize": "12px", "marginBottom": "12px"}),
                    html.Div(id="signals-table") # Will be populated by callback
                ],
                style=GLASS_CARD_STYLE
            )
        ]
    )
